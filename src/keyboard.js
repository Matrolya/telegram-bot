const keyboardButtons = require('./k-buttons')

module.exports = {
  home: [
    [keyboardButtons.home.inline, keyboardButtons.home.bag]
  ],
  inline: [
    [keyboardButtons.inline.valentine],
    [keyboardButtons.inline.office, keyboardButtons.inline.powder],
    [keyboardButtons.back]
  ]
}